# Secrete — NICIODATĂ în git
.env
.env.*
!.env.example
*.key

# Node / Python
node_modules/
venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# Build / deploy artefacte (generate din surse)
dist/
farabaliverne-deploy.zip

# closcu.html E versionata, ca index.html si politicieni.html: e o pagina
# generata pe care vrem sa o publicam. Comutatorul CLOSCU_ENABLED ramane, ca
# generarea sa fie o decizie explicita, nu un efect secundar.

# Propunerile redactorului local sunt ciorne, nu continut publicabil.
local/propuneri/
# Corpusul se regenereaza din data/ cu scripts/corpus_local.py — nu-l versionam
# (2 MB duplicat din articole, se reface intr-o secunda).
local/corpus.jsonl
local/stil.md
.venv/

# Documente INTERNE — raman pe disc, NU in git (repo-ul e public).
# Copie de siguranta in ~/Documents/Backup-farabaliverne/
WORKLOG.md
CLAUDE.md
