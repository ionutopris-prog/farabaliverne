# Secrete — NICIODATĂ în git
.env
.env.*
!.env.example
*.key

# Node / Python
node_modules/
venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# Build / deploy artefacte (generate din surse)
dist/
farabaliverne-deploy.zip

# Pagina Closcu e GENERATA si e in spatele CLOSCU_ENABLED. Nu se versioneaza:
# altfel un "git add -A" o urca, iar primul deploy o publica — exact ce s-a
# intamplat pe 16 august. Se regenereaza oricand cu CLOSCU_ENABLED=1.
clocsu.html
closcu.html
